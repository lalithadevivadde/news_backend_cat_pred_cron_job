name: News Mongo
on:
  schedule:
    - cron: '*/20 * * * *'  # Runs every 20 minutes
  workflow_dispatch:
    
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: News Mongo 
        run: curl -m 600 ${{ secrets.URL }}
